# --- Núcleo numérico / ML ---
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
torch>=2.2          # CPU por defecto; ver README para versión CUDA
pyyaml>=6.0
joblib>=1.3

# --- Conexión a Azure SQL ---
sqlalchemy>=2.0
pyodbc>=5.1         # requiere "ODBC Driver 18 for SQL Server" instalado en el SO
python-dotenv>=1.0

# --- Visualización / utilidades (opcional) ---
matplotlib>=3.8
tqdm>=4.66
