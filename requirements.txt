# ============================================================
#  Dependencias del proyecto BayesLSTM-VAE
#  Versiones fijadas al entorno validado.
# ============================================================

# --- Núcleo numérico / ML (fijadas a tu entorno) ---
numpy==2.5.0
pandas==3.0.3
scikit-learn==1.9.0
sqlalchemy==2.0.51

# torch: el sufijo +cpu/+cuXXX NO existe en PyPI estándar. Si este pin
# falla en otra máquina o en Colab, instala torch por su índice:
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cpu
torch==2.12.1

# --- Conexión a Azure SQL ---
pyodbc>=5.1            # requiere "ODBC Driver 18 for SQL Server" en el SO
python-dotenv>=1.0

# --- Configuración / utilidades ---
pyyaml>=6.0
joblib>=1.3

# --- Notebook / gráficos (opcional) ---
matplotlib>=3.8